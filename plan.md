# 実装計画（Pomodoro Timer）

## フェーズ概要

### Phase 1: プロジェクト基盤の整備
**目標**: 何も動かないが、開発を始める準備が整っている状態

- [x] Flaskアプリの初期化（`app.py` の骨格）
- [x] 静的ファイル配信の設定
- [x] SQLiteのテーブル作成・初期化
- [x] Tailwind CSS セットアップ（CDN + インライン設定）
- [x] カラートークン定義（色を絞る）
- [x] `requirements.txt` の整備
- [x] `GET /` でベースHTMLを返す最小Flask実装

**完了基準**: `flask run` でTailwindが効いた空ページが表示される

---

### Phase 2: タイマーUI（静的）
**目標**: UIモックを忠実に再現する。タイマーはまだ動かない

- [x] 円形プログレスリング（SVG）
- [x] 残り時間の中央テキスト表示（固定値）
- [x] フェーズ名表示（「作業中」固定）
- [x] 開始・リセットボタン
- [x] 当日進捗カード（固定値）
- [x] Tailwindでモックに近いスタイル仕上げ

**完了基準**: 見た目がUIモックと一致している

---

### Phase 3: タイマーロジック（フロントのみ）
**目標**: タイマーが実際に動く。記録はまだしない

- [x] `TimerCore`（状態遷移・時間計算の純粋ロジック）
- [x] 状態機械（idle / running / paused / completed）
- [x] `endTimestamp - now` 方式の残り時間計算
- [x] `UIBinder`（残り時間、リング進捗、ボタン状態をDOMへ反映）
- [x] フェーズ完了の検知と次フェーズへの自動遷移
- [x] バックグラウンド復帰時のズレ補正

**完了基準**: タイマーが開始・一時停止・リセット・完了まで正常に動く

---

### Phase 4: ブラウザ復元と通知
**目標**: ページを閉じても状態が保たれる。完了時に通知が来る

- [x] `localStorage` によるタイマー状態の保存・復元
- [x] ページ読込時に進行中状態を復元
- [x] フェーズ完了時のブラウザ通知（Notification API）
- [x] 通知許可のリクエスト処理

**完了基準**: リロードしても作業中タイマーが再開でき、完了時に通知が届く

---

### Phase 5: バックエンド連携（記録・統計）
**目標**: セッションが保存され、当日の進捗が実データで表示される

- [x] `GET /api/config`: タイマー設定値を返却
- [x] `POST /api/sessions`: 完了セッションを記録（`SessionService`, `SessionRepository`）
- [x] `GET /api/stats/today`: 当日の完了数・集中時間を集計して返却
- [x] フロント側の `ApiClient` 実装
- [x] 当日進捗カードをAPIデータに連動
- [x] API保存失敗時のローカル保持とリトライ

**完了基準**: 作業セッション完了→DBに保存→進捗カードが更新される一連フローが動く

---

### Phase 6: テスト整備・品質向上
**目標**: 主要ロジックにテストが揃い、壊れにくい状態になる

- [x] `pytest` でユニットテスト（状態遷移、時間計算、サービス集計）
- [x] `pytest` でAPIの統合テスト
- [x] Playwright でE2Eテスト（開始→完了→統計反映のハッピーパス）
- [x] テスト詳細設計は本フェーズ開始時に確認しながら決定

**完了基準**: CIで主要テストが通る

---

## フェーズ間の依存関係

```
Phase 1（基盤）
  └── Phase 2（静的UI）
        └── Phase 3（タイマーロジック）
              ├── Phase 4（復元・通知）
              └── Phase 5（バックエンド連携）
                    └── Phase 6（テスト整備）
```

Phase 4 と Phase 5 は Phase 3 通過後に並行着手可能。

---

## 判断が必要なタイミング

| タイミング | 内容 |
|-----------|------|
| Phase 1 前 | Tailwindのビルド方式（CDN vs ローカルビルド）の最終決定 |
| Phase 3 前 | SVGリングの実装方式（手書き vs ライブラリ）の確認 |
| Phase 4 | サウンド通知を入れるか否かの判断 |
| Phase 6 前 | テスト詳細設計（カバレッジ目標、命名規則など）を確認しながら決定 |

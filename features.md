# 実装機能一覧（Pomodoro Timer）

## 1. タイマー機能（コア）

- [ ] 作業タイマーの開始・一時停止・再開・リセット
- [ ] 残り時間の1秒ごとカウントダウン表示（`MM:SS` 形式）
- [ ] タイマー完了の検知と次フェーズへの遷移
- [ ] フェーズ管理（作業 → 休憩 → 作業 … の繰り返し、長休憩周期）
- [ ] バックグラウンド復帰時の時刻ズレ補正（`endTimestamp - now` 方式）
- [ ] ブラウザ再読込後のタイマー状態復元（`localStorage`）

## 2. UI表示機能

- [ ] 円形プログレスリングのアニメーション（経過率に連動）
- [ ] 現在フェーズ名の表示（「作業中」「休憩中」など）
- [ ] 残り時間の中央テキスト表示
- [ ] 開始・一時停止・リセットボタンの表示と状態切替

## 3. 当日進捗表示

- [ ] 完了した作業セッション数の表示
- [ ] 当日の合計集中時間（分）の表示
- [ ] ページ読込時に最新統計をAPIから取得して反映

## 4. 通知機能

- [ ] フェーズ完了時のブラウザ通知（Notification API）
- [ ] 通知許可のリクエスト処理
- [ ] サウンド通知（任意、実装フェーズで判断）

## 5. 設定管理

- [ ] タイマー設定値の提供（作業時間、休憩時間、長休憩時間、長休憩周期）
- [ ] Flask API（`GET /api/config`）からの設定取得

## 6. セッション記録

- [ ] 作業セッション完了時のバックエンドへの保存（`POST /api/sessions`）
- [ ] 保存失敗時のリトライまたはローカル保持

## 7. 統計API

- [ ] 当日の完了セッション数集計
- [ ] 当日の集中時間合計集計（`GET /api/stats/today`）

## 8. Tailwind CSS / UIスタイリング

- [ ] カラートークン定義（`tailwind.config.js`）
- [ ] 各コンポーネントのスタイル実装（カード、ボタン、プログレスリング）
- [ ] レスポンシブ対応（モバイル/PC）

## 9. インフラ・プロジェクト基盤

- [ ] Flaskアプリ初期化と静的ファイル配信
- [ ] SQLiteのテーブル作成・初期化（マイグレーション）
- [ ] Tailwindビルド設定（`postcss.config.js`）
- [ ] 依存関係管理（`requirements.txt`, `package.json`）

---

## 優先度整理

| 優先度 | 機能 |
|--------|------|
| 必須 | タイマー機能、UI表示、設定管理、Tailwind基盤、Flaskセットアップ |
| 重要 | セッション記録、統計表示、ブラウザ復元 |
| 任意 | 通知、サウンド、レスポンシブ詳細調整 |
